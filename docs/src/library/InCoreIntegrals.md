# InCoreIntegrals

In-core one- and two-electron integrals ([`InCoreInts`](@ref)) plus the
transformations, contractions, and orbital subsetting used to build the local
quantities that the cluster methods consume.

```@index
Pages = ["InCoreIntegrals.md"]
```

## Documentation
```@autodocs
Modules = [TPSChem.InCoreIntegrals]
Order   = [:type, :function]
```
